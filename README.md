# Admin Web UI - ระบบจัดการข้อมูลลูกค้า

ระบบ Admin Web UI สำหรับจัดการข้อมูลลูกค้าและธุรกรรมการโอนเงินระหว่างประเทศ พร้อมฟังก์ชันการพิมพ์ใบเสร็จและแบบฟอร์มลงทะเบียน

## คุณสมบัติหลัก

- 🔐 ระบบ Login สำหรับ Admin (admin01-admin05 / pass1234)
- 📝 บันทึกข้อมูลลูกค้าและรายละเอียดการโอนเงิน
- 🧾 Preview และ Print ใบเสร็จ
- 📄 Preview และ Print แบบฟอร์มลงทะเบียน

## ความต้องการของระบบ (System Requirements)

### Ubuntu / Linux

- Ubuntu 20.04 LTS หรือใหม่กว่า
- Node.js 18.x หรือใหม่กว่า
- npm 9.x หรือใหม่กว่า

## วิธีการติดตั้งบน Ubuntu

### 1. ติดตั้ง Node.js และ npm

```bash
# อัพเดท package list
sudo apt update

# ติดตั้ง curl (ถ้ายังไม่มี)
sudo apt install curl -y

# ติดตั้ง Node.js 18.x จาก NodeSource
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# ตรวจสอบเวอร์ชัน
node --version
npm --version
```

### 2. ติดตั้ง Git (ถ้ายังไม่มี)

```bash
sudo apt install git -y
git --version
```

### 3. Clone โปรเจกต์

```bash
# Clone repository (ถ้ามี)
git clone <repository-url>
cd admin-web-ui

# หรือ ถ้าได้โค้ดมาแล้ว ให้ cd เข้าไปในโฟลเดอร์โปรเจกต์
cd /path/to/admin-web-ui
```

### 4. ติดตั้ง Dependencies

```bash
# ติดตั้ง packages ทั้งหมด
npm install

# รอจนกว่าการติดตั้งจะเสร็จสมบูรณ์
```

### 5. รันโปรเจกต์

```bash
# รันในโหมด Development
npm run dev
```

เมื่อรันสำเร็จจะเห็นข้อความคล้ายๆ นี้:

```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.1.100:3000/
```

### 6. เปิดเว็บไซต์

เปิดเว็บเบราว์เซอร์แล้วเข้าไปที่:

```
http://localhost:3000
```

## การใช้งาน

### การ Login

- Username: `admin01`, `admin02`, `admin03`, `admin04`, หรือ `admin05`
- Password: `pass1234`

### เมนูหลัก

1. **Dashboard** - หน้าภาพรวมข้อมูล
2. **บันทึกข้อมูลลูกค้า** - กรอกข้อมูลลูกค้าและธุรกรรม
3. **จัดการใบเสร็จ** - ดู แก้ไข ลบใบเสร็จ
4. **Print แบบฟอร์ม** - พิมพ์เอกสารต่างๆ

### การพิมพ์เอกสาร

1. กรอกข้อมูลในฟอร์มให้ครบถ้วน
2. คลิก "Preview ใบเสร็จ" หรือ "Preview แบบฟอร์มลงทะเบียน"
3. คลิกปุ่ม "Print" หรือ "Download PDF"

## Commands ที่ใช้บ่อย

```bash
# รันโปรเจกต์ในโหมด Development
npm run dev

# Build โปรเจกต์สำหรับ Production
npm run build

# Preview โปรเจกต์ที่ build แล้ว
npm run preview

# ตรวจสอบ Lint
npm run lint
```

## การ Build สำหรับ Production

```bash
# Build โปรเจกต์
npm run build

# ไฟล์ที่ build เสร็จจะอยู่ในโฟลเดอร์ dist/
# สามารถนำไฟล์ในโฟลเดอร์ dist/ ไป deploy บน web server ได้
```

### Deploy ด้วย Nginx (ตัวอย่าง)

```bash
# ติดตั้ง Nginx
sudo apt install nginx -y

# Copy ไฟล์ที่ build แล้วไปยัง Nginx directory
sudo cp -r dist/* /var/www/html/

# Restart Nginx
sudo systemctl restart nginx

# เปิดเว็บไซต์ที่ http://your-server-ip
```

## การแก้ปัญหา (Troubleshooting)

### ปัญหา: Port 3000 ถูกใช้งานอยู่แล้ว

```bash
# หา process ที่ใช้ port 3000
sudo lsof -i :3000

# Kill process (แทนที่ PID ด้วยตัวเลขที่ได้)
kill -9 <PID>

# หรือ เปลี่ยน port ในไฟล์ vite.config.ts
```

### ปัญหา: npm install ไม่สำเร็จ

```bash
# ลบ node_modules และ package-lock.json
rm -rf node_modules package-lock.json

# ติดตั้งใหม่
npm install
```

### ปัญหา: Permission denied

```bash
# ถ้าเจอปัญหา permission ในการติดตั้ง global packages
sudo chown -R $USER:$USER ~/.npm
sudo chown -R $USER:$USER /usr/local/lib/node_modules
```

## โครงสร้างโปรเจกต์

```
admin-web-ui/
├── components/           # React components
│   ├── CustomerForm.tsx
│   ├── Dashboard.tsx
│   ├── Header.tsx
│   ├── Login.tsx
│   ├── ReceiptPreview.tsx
│   ├── RegistrationFormPreview.tsx
│   └── Sidebar.tsx
├── styles/              # CSS files
│   └── globals.css
├── App.tsx              # Main app component
├── main.tsx             # Entry point
├── index.html           # HTML template
├── package.json         # Dependencies
├── vite.config.ts       # Vite configuration
├── tsconfig.json        # TypeScript configuration
└── README.md            # Documentation
```

## เทคโนโลยีที่ใช้

- **React 18** - UI Library
- **TypeScript** - Type-safe JavaScript
- **Vite** - Build tool และ Dev server
- **Tailwind CSS** - Utility-first CSS framework
- **Lucide React** - Icon library

## ข้อมูล Admin สำหรับทดสอบ

| Username | Password |
| -------- | -------- |
| admin01  | pass1234 |
| admin02  | pass1234 |
| admin03  | pass1234 |
| admin04  | pass1234 |
| admin05  | pass1234 |

## หมายเหตุ

- ข้อมูลทั้งหมดจะเก็บใน Browser Local Storage
- ไม่มี Backend server (Frontend only)
- ไม่เหมาะสำหรับเก็บข้อมูลจริงหรือข้อมูลที่เป็นความลับ
- สำหรับใช้งานจริง ควรพัฒนา Backend API เพิ่มเติม

## License

MIT License

## ติดต่อ / สนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ กรุณาติดต่อทีมพัฒนา